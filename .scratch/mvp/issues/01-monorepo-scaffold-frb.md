Category: enhancement
Status: done

# Monorepo 脚手架与 FRB 连通

## What to build

建立 `app/`（Flutter，支持 desktop + mobile，不含 web）与 `core/`（Rust library）并列的 monorepo 骨架，配置 flutter_rust_bridge，使 Flutter 能调用 Rust 并收到响应。这是后续所有切片的集成基础。

## Acceptance criteria

- [ ] 仓库根目录存在 `app/` 与 `core/`，符合 ADR-0004
- [ ] `core/` 为 Rust library crate，可被 FRB 暴露
- [ ] Flutter 应用可在至少一个桌面目标上构建运行
- [ ] Flutter UI 有可见方式触发 Rust 调用并展示成功结果（连通性验证即可）
- [ ] FRB 代码生成流程有文档或脚本，新开发者可复现

## Blocked by

None - can start immediately

## Comments

> *This was generated by AI during triage.*

### Triage — 2026-06-01

批量状态更新：对照当前代码库（`app/`、`core/`、`scripts/generate-frb.ps1`），标记为 `done`。
