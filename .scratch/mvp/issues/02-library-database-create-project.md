Category: enhancement
Status: done

# Library Database 与 Create Project 首条链路

## What to build

在 Core 中初始化应用沙盒内的 Library Database（`library.db`），实现 Create Project：在库中创建无 Page 的 Project、默认 Metadata（标题「未命名」）、对应 Project Storage 目录。Flutter Library 首屏从 Core 读取 Project 列表，提供「新建」入口，创建后列表即时更新。

## Acceptance criteria

- [ ] Library Database schema 覆盖 Project、Metadata 及 Library 列表所需字段，符合 ADR-0003
- [ ] Create Project 在 `{app_data}/projects/{project_id}/` 创建 Project Storage（含 `assets/`、`.cache/`）
- [ ] Core 暴露 FRB API：`createProject`、`listProjects`（至少含 id、标题、修改时间）
- [ ] Flutter Library 首屏展示 Project 列表（可为空）
- [ ] 用户点击「新建」后，空 Project 出现在列表中，可点击进入（编辑器可为占位页）

## Blocked by

None — `.scratch/mvp/issues/01-monorepo-scaffold-frb.md` is `done`.

## Comments

> *This was generated by AI during triage.*

### Triage — 2026-06-01

批量状态更新：Library DB、`createProject`/`listProjects`、库首屏已实现，标记为 `done`。
