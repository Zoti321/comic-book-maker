Category: enhancement
Status: done

# 删除 Project

## What to build

Core 提供 `delete_project`：删除 Library Database 中的 Project 记录（`pages` 级联删除）并移除 `Project Storage` 目录。Export 支持 `delete_project_after_export`，导出成功后可选删除项目。Flutter：库页长按删除、导出对话框勾选「导出后删除」。

## Acceptance criteria

- [x] `delete_project` 删除 DB 行与磁盘存储
- [x] 导出成功且勾选时删除项目；导出失败不删除
- [x] 库页可手动确认删除
- [x] 导出后可返回库页

## Blocked by

- `.scratch/mvp/issues/09-export-cbz.md`

## Comments

> *This was generated by AI during triage.*

### Triage — 2026-06-01

批量状态更新：本票在对话中实现，对照 `core/src/db/mod.rs`、`core/src/api/simple.dart` 与 `library_page.dart` / `project_editor_page.dart`，标记为 `done`。
