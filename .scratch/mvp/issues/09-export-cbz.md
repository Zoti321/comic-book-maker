Category: enhancement
Status: done

# Export CBZ

## What to build

用户从 Project 编辑器 Export 为 CBZ Archive Format：Core 从 Library Database 与 Project Storage 读取 Page 与 Metadata，生成含 ComicInfo.xml 的 CBZ，Page Image 以原格式打包。Export 不修改 Project 或 Library Database 中的编辑状态。

## Acceptance criteria

- [ ] Export 生成可被常见阅读器打开的 CBZ
- [ ] CBZ 内含 ComicInfo.xml，字段来自 Metadata
- [ ] Page Image 按 Project 页序打包，保持原格式（不转码）
- [ ] Project 无 Page 时 Export 被拒绝并提示至少需一页
- [ ] Export 后 Project 内数据与 Export 前一致（无静默修改）
- [ ] 用户可选择 Export 目标路径（桌面）；移动端写入用户可选/share 位置

## Blocked by

None — dependencies are `done`.

## Comments

> *This was generated by AI during triage.*

### Triage — 2026-06-01

批量状态更新：CBZ 导出与 ComicInfo 已实现，标记为 `done`。另见 `.scratch/mvp/issues/11-delete-project.md`（导出后/手动删除项目，超出本票原始范围）。
