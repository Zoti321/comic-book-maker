Category: enhancement
Status: done

# Metadata 编辑

## What to build

Flutter Metadata 编辑面板与 Core 持久化逻辑，支持 v1 书目字段的读取与更新。Metadata 存于 Library Database，不写入 Project Storage 内的 ComicInfo.xml。

## Acceptance criteria

- [ ] 可编辑字段：标题、系列名、卷号、期号、作者、画师、出版社、出版日期、语言、简介
- [ ] `cover_page_index` 字段存在，默认 0
- [ ] 修改后持久化到 Library Database，重进 Project 后值保持
- [ ] Create Project 默认标题为「未命名」，可被用户修改
- [ ] UI 与 Core 使用 CONTEXT.md 中的 Metadata 术语

## Blocked by

None — dependencies are `done`.

## Comments

> *This was generated by AI during triage.*

### Triage — 2026-06-01

批量状态更新：Metadata 读写与编辑器面板已实现，标记为 `done`。
