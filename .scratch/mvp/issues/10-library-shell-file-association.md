Category: enhancement
Status: done

# Library 完整壳层

## What to build

完善 Library 作为应用首屏：`AppShell` 导航、Import CBZ/CBR 入口、Create Project 入口、按最近打开时间排序的 Project 列表。

~~桌面端注册 `.cbz` / `.cbr` 文件关联~~（**已取消**，见 `docs/adr/0005-decline-desktop-archive-file-association.md`）。

## Acceptance criteria

- [x] 应用启动默认进入 Library（`AppShell` → 项目页）
- [x] Library 提供显式 Import CBZ/CBR 与 Create Project 入口
- [x] Project 列表按最近打开时间排序（`touch_project`）
- [x] 移动端无文件关联时不报错，Import 仍通过 Library 入口可用

## Cancelled (out of scope)

- ~~桌面端双击 `.cbz` / `.cbr` 唤起应用并完成 Import~~
- ~~文件关联 Import 与 Library 内 Import 行为一致~~

## Blocked by

None — dependencies are `done`.

## Comments

> *This was generated by AI during triage.*

### Triage — 2026-06-01

维护者取消桌面 `.cbz`/`.cbr` 文件关联（见 `docs/adr/0005-decline-desktop-archive-file-association.md`）。壳层、侧栏、库 Import/新建、最近打开排序已实现，本票标记为 `done`。
